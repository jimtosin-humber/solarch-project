{
  "queryStringParameters": {
    "start": "$input.params('group')",
    "count": "$input.params('count')",
    "sortField": "$input.params('sortField')",
    "sortOrder": "asc",
    "searchString": "$input.params('searchString')"
  }
}